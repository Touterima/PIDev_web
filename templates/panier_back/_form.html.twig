{{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
    <div class="form-group">
        {{ form_label(form.total, 'Total', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_widget(form.total, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.total) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.datepanier, 'Date Panier', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_widget(form.datepanier, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.datepanier) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.etat, 'Etat', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_widget(form.etat, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.etat) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.idUser, 'ID User', {'label_attr': {'class': 'col-sm-2 control-label'}}) }}
        <div class="col-sm-10">
            {{ form_widget(form.idUser, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.idUser) }}
        </div>
    </div>

    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
        </div>
    </div>
{{ form_end(form) }}
