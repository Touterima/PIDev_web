<form method="post" action="{{ path('app_nutrition_back_delete', {'nutritionId': nutrition.nutritionId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nutrition.nutritionId) }}">
    <button class="btn">Delete</button>
</form>
