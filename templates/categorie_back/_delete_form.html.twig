<form method="post" action="{{ path('app_categorie_back_delete', {'categorieId': categorie.categorieId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.categorieId) }}">
    <button class="btn">Delete</button>
</form>
